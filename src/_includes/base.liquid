<!doctype html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{{ title }}</title>
        <link rel="stylesheet" href="/base_styles.css">
        <!-- page specific imports -->
        {% block style %}
        {% endblock %}
        <!-- end page specific imports -->
    </head>
    <body>
        {% block content %}
        {% endblock %}
    </body>
</html>
